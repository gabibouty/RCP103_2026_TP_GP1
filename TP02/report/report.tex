\documentclass[11pt,a4paper]{article}

% ---------------------------------------------
% ENCODAGE / LANGUE
% ---------------------------------------------
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[french]{babel}

% ---------------------------------------------
% MISE EN PAGE
% ---------------------------------------------
\usepackage[a4paper,margin=2.5cm]{geometry}
\usepackage{setspace}
\usepackage{parskip}

% ---------------------------------------------
% MATHS / IMAGES / TABLEAUX
% ---------------------------------------------
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{float}

% ---------------------------------------------
% LIENS
% ---------------------------------------------
\usepackage[colorlinks=true,linkcolor=black,urlcolor=blue,citecolor=black]{hyperref}

% ---------------------------------------------
% CODE (optionnel)
% ---------------------------------------------
\usepackage{xcolor}
\usepackage{listings}
\renewcommand{\lstlistingname}{Code}

\definecolor{backcolour}{rgb}{0.95,0.95,0.95}
\definecolor{codegreen}{rgb}{0,0.5,0}
\definecolor{codegray}{rgb}{0.4,0.4,0.4}

\lstdefinestyle{mystyle}{
    backgroundcolor=\color{backcolour},
    commentstyle=\color{codegreen},
    numberstyle=\tiny\color{codegray},
    basicstyle=\ttfamily,
    breaklines=true,
    numbers=left,
    numbersep=5pt,
    showstringspaces=false
}

\lstset{
    basicstyle=\ttfamily,
    backgroundcolor=\color{gray!10},
    frame=single,
    breaklines=true,
    captionpos=b
}

\lstset{
  literate=
  {á}{{\'a}}1 {é}{{\'e}}1 {í}{{\'i}}1 {ó}{{\'o}}1 {ú}{{\'u}}1
  {Á}{{\'A}}1 {É}{{\'E}}1 {Í}{{\'I}}1 {Ó}{{\'O}}1 {Ú}{{\'U}}1
  {à}{{\`a}}1 {è}{{\`e}}1 {ì}{{\`i}}1 {ò}{{\`o}}1 {ù}{{\`u}}1
  {À}{{\`A}}1 {È}{{\'E}}1 {Ì}{{\`I}}1 {Ò}{{\`O}}1 {Ù}{{\`U}}1
  {ä}{{\"a}}1 {ë}{{\"e}}1 {ï}{{\"i}}1 {ö}{{\"o}}1 {ü}{{\"u}}1
  {Ä}{{\"A}}1 {Ë}{{\"E}}1 {Ï}{{\"I}}1 {Ö}{{\"O}}1 {Ü}{{\"U}}1
  {â}{{\^a}}1 {ê}{{\^e}}1 {î}{{\^i}}1 {ô}{{\^o}}1 {û}{{\^u}}1
  {Â}{{\^A}}1 {Ê}{{\^E}}1 {Î}{{\^I}}1 {Ô}{{\^O}}1 {Û}{{\^U}}1
  {œ}{{\oe}}1 {Œ}{{\OE}}1 {æ}{{\ae}}1 {Æ}{{\AE}}1
  {ç}{{\c{c}}}1 {Ç}{{\c{C}}}1 {¿}{{\textquestiondown}}1 {¡}{{\textexclamdown}}1
}

% ---------------------------------------------
% TITLE
% ---------------------------------------------
\title{\textbf{RCP103 -- Rapport de TP}\\TP02 - Création d'un simulateur à évènements discrets}
\author{BOURDAYA Amina, BOUTY Gabriel, CLERC Julien}
\date{\today}

% ---------------------------------------------
% DOCUMENT
% ---------------------------------------------
\begin{document}

\maketitle

\bigskip

\begin{abstract}
    Ce TP a pour objectif de créer un programme permettant de simuler un système simple de type Client(s)/Serveur asynchrones. Ce simulateur nous permettra de réaliser des mesures variées pour qualifier notre système (nombre instantané de requêtes, temps d'attente, etc.), sans avoir à mettre en œuvre concrètement ce dernier.
\end{abstract}

\bigskip

% ---------------------------------------------
\section{Description du système simulé}

Comme évoqué en introduction, ce rapport présente la réalisation d'un simple simulateur à évènements discrets du type Client(s)/Serveur:

Un ensemble de $N$ clients ($N \geq 1$), envoient des messages à 1 serveur. Un portail sert d'intermédiaire entre les clients et le serveur. Compte tenu que le serveur ne peut traiter qu'un message à la fois, le portail permet de temporiser les messages en attente de traitement dans une file de taille fixe $K$.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.80\textwidth]{../images/system.png}
    \caption{Schéma du système simulé}
\end{figure}

Dans ce simulateur, les envois de messages suivent un processus de Poisson: le taux d'envois est constant dans le temps, les temps entre deux envois consécutifs suivent une distribution exponentielle. De même, le temps de service suit une loi exponentielle.

Compte tenu du fait que nous n'avons pour l'instant qu'un seul serveur, nous pouvons donc définir cette simulation comme un processus de file d'attente de type M/M/1/K:

\begin{itemize}
    \item M: Arrivées selon un processus de Poisson (Markovien).
    \item M: Temps de service selon une distribution exponentielle (Markovien).
    \item 1: Nombre de serveurs (ici, 1).
    \item K: Capacité du système (ici, la taille de la file du portail de capacité K).
\end{itemize}

La configuration du simulateur est définie comme suit:

\begin{itemize}
    \item Temps moyen inter-message: configurable
    \item Temps de transmission: $1t$
    \item Temps moyen de service: configurable
    \item Temps total de la simulation: configurable
\end{itemize}

\bigskip
\bigskip
\bigskip

% ---------------------------------------------
\section{Architecture du simulateur}
\subsection{Architecture initiale}

Afin de simuler les transmissions et traitement de messages entre le ou les \emph{Client} et le \emph{Server} (à travers le \emph{Gateway}), chaque étape de gestion du \emph{Message} sera encapsulée dans des \emph{Event}. Ces \emph{Event} pourront être, de fait, de 3 types différents:

\begin{itemize}
    \item \lstinline|SEND_MSG|: Le message est envoyé par le client.
    \item \lstinline|RECV_MSG|: Le message est reçu par le portail.
    \item \lstinline|MSG_DPT|: Le message est transmis au serveur (si ce dernier est disponible).
\end{itemize}

Chacun de ces \emph{Event} sera timestampé et stocké chronologiquement dans un ordonnanceur (\emph{Scheduler}). Ainsi, en ajoutant progressivement des évènements à notre \emph{Scheduler}, et en itérant  sur ces derniers, nous pourrons simuler l'écoulement du temps, en nous concentrant uniquement sur les échantillons temporels qui nous intéressent.

L'ensemble de ces composants seront encapsulés dans un \emph{Engine}, dont la boucle principale viendra interagir avec ces derniers afin de dérouler la simulation.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.80\textwidth]{../images/engine_view.png}
    \caption{Schéma de principe du simulateur}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.68\textwidth]{../images/run_algo.png}
    \caption{Algorithme de la boucle principale de l'\emph{Engine}}
\end{figure}

\bigskip

\subsection{Organisation du code source}

Le dossier fourni s'organise ainsi:
\begin{itemize}
    \item \lstinline|root|: Le point d'entrée du programme principal (\lstinline|tp02_main.py|), les fichiers de configurations de lancement des tests, ainsi que les dépendances Python.
    \item Dossier \lstinline|sources|: Le code applicatif.
    \item Dossier \lstinline|tests|: Le code relatif aux tests unitaires.
    \item Dossiers \lstinline|report| et \lstinline|images|: Le présent rapport et les images associées.
\end{itemize}

\bigskip
\bigskip
\bigskip
\bigskip

% ---------------------------------------------
\section{Création des premières classes}
\subsection{Les données: class Message et class Event}
Comme évoqué précédemment, la classe \emph{Message} est l'unité d'échange entre \emph{Client} et \emph{Server}.
Cette classe contient:
\begin{itemize}
    \item L'identifiant du message: \lstinline|message_id|
    \item L'identifiant du nœud émetteur: \lstinline|source|
    \item L'identifiant du nœud destination: \lstinline|destination|
    \item L'instant auquel le message est envoyé par le client: \lstinline|send_time|
    \item L'instant auquel le message arrive à la passerelle: \lstinline|arrival_time|
    \item L'instant auquel le traitement du message commence par le serveur : \lstinline|server_start|
\end{itemize}

Ce \emph{Message} est encapsulé dans un \emph{Event}, dont les attributs sont les suivants:
\begin{itemize}
    \item L'identifiant de l'évènement: \lstinline|id|
    \item Le type de l'évènement: \lstinline|type|
    \item Le \emph{Message} encapsulé: \lstinline|message|
\end{itemize}
\emph{Event} possède par ailleur un accesseur permettant de déterminer son timestamp, en fonction de son type.

\bigskip

\subsection{L'ordonnanceur: class Scheduler}

La classe \emph{Scheduler} est chargée de gérer les futurs évènements du simulateur. Elle contient une liste d'évènements triés par ordre chronologique.

Lorsqu'un nouvel évènement est ajouté, la méthode parcourt la liste depuis la fin afin de trouver la bonne position d'insertion. Tant que le temps de l'évènement déjà présent est supérieur au temps du nouvel évènement, on remonte la liste. Lorsque la bonne position est trouvée, le nouvel évènement est inséré avec insert. Cela permet de garder la liste triée sans utiliser \lstinline|sort| après chaque ajout.

La méthode \lstinline|pop_event| retire et retourne le premier évènement de la liste. Comme la liste est maintenue triée par temps d'exécution, cet évènement correspond toujours au prochain évènement à traiter.

La méthode \lstinline|get_current_time| retourne le temps du premier évènement de la liste, c'est-à-dire le temps courant du simulateur. Le \emph{Scheduler} possède une mémoire du timestamp du dernier évènement retiré, afin de fournir toujours un temps de référence, même si sa liste d'évènement est vide.

La méthode \lstinline|has_events| permet de savoir s'il reste encore des évènements à traiter.

Enfin, le \emph{Scheduler} enregistre progressivement les évènements retirés, afin de pouvoir garder une trace de ces derniers.

\bigskip

\subsection{Les composants simulés: class Client et Server}

Les classes \emph{Client} et \emph{Server} représentent les composants princiaux du système simulé. \emph{Client} émet les messages qui seront traités par \emph{Server}.

Comme décrit précédemment, les temps d'émissions comme les temps de traitement sont définis par une distribution exponentielle. Ainsi, on pourra définir pour chaques instances de \emph{Client} et \emph{Server}, une moyenne: pour \emph{Client}, cette valeur correspond au nombre moyen de message émis par unité de temps, et pour \emph{Server}, cette valeur correspond au nombre moyen de message traités par unité de temps.

\emph{Client} expose quelques fonctions minimales:
\begin{itemize}
    \item \lstinline|get_next_msg_time|: permettant de connaitre le timestamp du prochain envoi de \emph{Message} émis par le \emph{Client}.
    \item \lstinline|pop_message|: permettant de simuler l'envoi du \emph{Message}. Une instance préconfigurée de \emph{Message} est renvoyée, et le timestamp interne est incrémenté d'une valeur aléatoire, générée selon une distribution exponentielle.
\end{itemize}
\bigskip
\emph{Server} expose aussi quelques fonctions minimales:
\begin{itemize}
    \item \lstinline|is_free|: permettant de savoir si le \emph{Server} est actuellement en train de traiter virtuellement un \emph{Message}.
    \item \lstinline|start_work|: permettant de simuler le début du traitement d'un \emph{Message}. Un timestamp interne de fin de travail est définit selon le timestamp passé en argument, augmenté d'une valeur aléatoire, générée là aussi selon une distribution exponentielle.
\end{itemize}

\bigskip

\subsection{Les fonctions de logs: functions Trace}

Le module \lstinline|trace| contient trois fonctions fournissant un système de journalisation au simulateur, permettant de visualiser et d'enregistrer les évènements.

Voici le prototype et la description de chaque fonction :

\lstinline|def get_time_and_node(e: Event)|:  prend un évènement en paramètre et retourne un tuple constitué du numéro de nœud et du temps en fonction du type d'évènement.

\lstinline|def generateTraceOut(t_event: list[Event])|:  prend une liste d'évènements en paramètre et écrit sur la sortie standard l'ensemble des évènements contenu dans la liste.

\lstinline|def generateTraceCSV(t_event: list[Event])|: prend une liste d'évènements en paramètre et écrit dans un fichier nommé \lstinline|trace.csv| l'ensemble des évènements contenu dans la liste au format CSV. Le délimiteur utilisé est le caractère '\lstinline|;|'.

\bigskip

\subsection{Le cœur du système: class Engine}

La classe \emph{Engine} représente le cœur du simulateur. Elle définit les instances des différents composants (\emph{Client}, \emph{Server}, etc.) lors de son initialisation, et simule les transactions entre chacuns d'entre eux durant l'exécution de la simulation.

Sa fonction \lstinline|run| permet de lancer l'execution. Celle-ci s'appuie sur un \emph{Scheduler} pour déterminer l'évolulution du temps. Son rôle principale est de forcer les comportements de chacuns des composants (\emph{Client}, \emph{Server}, \emph{Queue}, etc.) et d'encapsuler les \emph{Message} émis, reçu, traités dans des \emph{Event}, afin d'alimenter le \emph{Scheduler}.

La classe \emph{Engine} propose aussi une fonction \lstinline|log| permettant d'afficher les \emph{Event} déjà passés, grâce aux fonctions
proposées par le module \emph{trace}.

\bigskip

\subsection{Le point d'entrée: function main}

Le point d'entrée du programme se trouve dans \lstinline|tp02_main.py|. Il permet d'executer le programme simplement en appelant \lstinline|python tp02_main.py| depuis le répertoire racine.

En l'état, il se contente de créer une instance d'\emph{Engine},
et de lancer l'execution de cette instance. Mais on pourrait par exemple imaginer y ajouter un parsing d'arguments pour configurer le simulateur dynamiquement.

\bigskip

\subsection{La file d'attente des évènements: class Queue}

La classe \lstinline|Queue| est une structure de donnée de type FIFO (First In First Out). Elle va garder les messages dans l'attente de leur prise en compte par le serveur.

Voici le prototype et la description de chaque méthodes :

\lstinline|def put(self, msg: Message)|: ajoute un message dans la file.

\lstinline|def get(self)|: récupère le message le plus ancien ajouté dans la file.

\lstinline|def is_empty(self)|: indique si la file est vide.

\lstinline|def size(self)|: renvoie la taille de la file c'est-à-dire le nombre de message.

\lstinline|def index(self, msg: Message)|: renvoie l'index d'un message dans la file. Cette méthode est utile pour les tests.

\bigskip

\bigskip
\bigskip

% ---------------------------------------------
\section{Tests des premières classes}
\subsection{Le système de test: Pytest}
Afin de vérifier le bon fonctionnement des classes implémentées, des tests unitaires ont été réalisés avec l'aide de l'utilitaire \lstinline|Pytest|. L'objectif est de tester chaque classe séparément avant de l'intégrer dans le simulateur. L'ensemble des tests sont regroupés dans le répertoire \lstinline|tests|.

\bigskip
\bigskip

\subsection{Présentation des tests rédigés}

Test de \emph{Message}:
\begin{itemize}
    \item Le test \lstinline|test_message| vérifie que le constructeur initialise correctement les attributs du message (l'identifiant, la source et la destination, les différents timestamp).
    \item Le test \lstinline|test_message_setters| vérifie que les setters modifient correctement les valeurs internes de l'objet, après modification de l'identifiant, de la source, de la destination, et des différents temps, les getters sont utilisés pour vérifier que les nouvelles valeurs ont bien été enregistrées.
    \item Le test \lstinline|test_message_str| vérifie la méthode d'affichage de la classe message. Cette méthode est utile pour le débogage, car elle permet d'afficher rapidement les informations principales d'un message.
\end{itemize}

\bigskip

Test de \emph{Event}:
\begin{itemize}
    \item Le test \lstinline|test_event_init| vérifie que le constructeur initialise correctement le temps de l'événement et son type. Un événement de type \lstinline|SEND_MSG| est créé avec un temps égal à 1.5, puis les getters permettent de vérifier que ces valeurs sont bien stockées.
    \item Le test \lstinline|test_event_setters| vérifie que le temps et le type d'un événement peuvent être modifiés correctement. Par exemple, un événement initialement de type \lstinline|SEND_MSG| est modifié en \lstinline|RECV_MSG|, et son temps est changé de 5.5 à 8.0.
    \item Le test \lstinline|test_event_message_content| vérifie que l'événement conserve bien le message qui lui est associé. Après avoir récupéré le message depuis l'événement, le test vérifie que son identifiant, sa source et sa destination sont corrects.
    \item Le test \lstinline|test_event_str| vérifie la méthode d'affichage de la classe \emph{Event}. Cette méthode affiche l'identifiant de l'événement, son type, son temps et le message associé. Elle permet donc de faciliter le suivi des événements.
\end{itemize}

\bigskip

Test de \emph{Server}:
\begin{itemize}
    \item Le test \lstinline|test_server| vérifie que la classe \emph{Server} rempli bien son rôle de simulation de serveur, à savoir pouvoir démarrer un job, rester occupé pendant une période de temps aléatoire, puis être à nouveau libre à la fin de cette période de temps.
\end{itemize}

\bigskip

Test de \emph{Client}:
\begin{itemize}
    \item Le test \lstinline|test_client_init| vérifie l'état initial d'un \emph{Client}.
    \item Les tests \lstinline|test_get_next_msg_time| et \lstinline|test_pop_message| vérifient que \emph{Client} renvoi bien un temps qui s'incrémente au fur et à mesure que les \emph{Message} sont virtuellement émis par le \emph{Client}.
\end{itemize}

\bigskip

Test de \emph{Scheduler}:
\begin{itemize}
    \item Le test \lstinline|test_sheduler| vérifie que les temps des événements récupérés sont toujours dans l'ordre chronologique croissant. Cela permet de valider le rôle principal du \emph{Scheduler} : maintenir les événements futurs triés par temps d'exécution. Le test vérifie aussi que les évènements passés sont bien enregistrés dans la liste des évènements passés.
\end{itemize}

\bigskip

Test de \emph{Trace}:
\begin{itemize}
    \item Le test définit par \lstinline|test_get_time_and_node_event_SEND_MSG| vérifie que la fonction \lstinline|get_time_and_node_event| renvoie bien le bon tuple en fonction du type de message.
    \item Le test définit par \lstinline|test_get_time_and_node_event_RECV_MSG| vérifie que la fonction \lstinline|get_time_and_node_event| renvoie bien le bon tuple en fonction du type de message.
    \item Le test définit par \lstinline|test_get_time_and_node_event_MSG_DEPT| vérifie que la fonction \lstinline|get_time_and_node_event| renvoie bien le bon tuple en fonction du type de message.
    \item Le test définit par \lstinline|test_generateTraceOut_SEND_MSG(capsys)| vérifie que la fonction \lstinline|generateTraceOut| affiche les bonnes informations en fonction du type de message.
    \item Le test définit par \lstinline|test_generateTraceOut_RECV_MSG(capsys)| vérifie que la fonction \lstinline|generateTraceOut| affiche les bonnes informations en fonction du type de message.
    \item Le test définit par \lstinline|test_generateTraceOut_MSG_DEPT(capsys)| vérifie que la fonction \lstinline|generateTraceOut| affiche les bonnes informations en fonction du type de message.
    \item Le test définit par \lstinline|test_generateTraceCSV| vérifie que la fonction \lstinline|generateTraceCSV| crée le fichier "trace.csv" avec les bonnes valeurs pour plusieurs messages.
\end{itemize}

\bigskip

Test de \emph{Engine}:
\begin{itemize}
    \item Le test \lstinline|test_engine_complete| vérifie que l'exécution du \lstinline|run| de \emph{Engine} ne tombe pas dans une boucle sans fin. Il vérifie ensuite que les \emph{Event} reçus suivent bien une logique temporelle incrémentale, et qu'ils soient bien cohérents les uns avec les autres. Par exemple, un \lstinline|MSG_DEPT| doit avoir un \lstinline|RECV_MSG|, ainsi qu'un \lstinline|SEND_MSG| correspondants avec des timestamp tel que $t\textsubscript{MSG\_DEPT} \geq t\textsubscript{RECV\_MSG} \geq t\textsubscript{SEND\_MSG}$.
\end{itemize}

\bigskip

Test de \emph{Queue}:
\begin{itemize}
    \item Le test définit par \lstinline|test_put| vérifie que la méthode \lstinline|put| ajoute correctement un message.
    \item Le test définit par \lstinline|test_get| vérifie que la méthode \lstinline|get| renvoie correctement un message et le supprime de la file d'attente.
    \item Le test définit par \lstinline|test_is_empty| vérifie que la méthode \lstinline|is_empty| renvoie le booléen True si la file est vide
    \item Le test définit par \lstinline|test_size| vérifie que la méthode \lstinline|size| renvoie la taille de la file.
    \item Le test définit par \lstinline|test_str| vérifie que la méthode \lstinline|__str__| renvoie la chaîne de caractère comprenant l'ensemble des éléments de la file d'attente.
\end{itemize}

\bigskip

\subsection{Exécution des tests}
Les tests peuvent être lancés depuis le répertoire principal en tapant la commande \lstinline|pytest| (attention, \lstinline|Pytest| doit être préalablement installé).
L'ensemble des tests présents dans le répertoire \lstinline|tests| seront exécutés.

\bigskip
\bigskip
\bigskip

% ---------------------------------------------
\section{Conclusion}
Ce TP nous permet d'appréhender ce que peut être un simulateur à évènements discrets. Nous mettons en œuvre des concepts vus en cours tels que les files d'attente ou encore les processus de Poisson.

Il nous permet aussi de comprendre l'intérêt que peut représenter la complémentarité qu'il peut exister entre modélisation mathématique et simulation. En effet, la modélisation mathématique nous fournit les outils (variables aléatoires, processus stochastiques, etc.) qui vont nous permettre de construire facilement un simulateur, et ainsi réaliser, tout aussi facilement, des mesures théoriques sur un système complexe. Établir et mettre en œuvre un protocole de mesures sur ce même système dans le monde réel aurait été bien plus lourd et couteux.

D'un point de vue du développement logiciel, les différentes itérations de réalisation, basées sur une approche TDD (Test Driven Developpement), nous permettent de valider progressivement nos composants logiciels. Cela facilite ensuite l'assemblage, et permet aussi d'éviter les régressions.

\end{document}
