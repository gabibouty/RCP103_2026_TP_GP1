# RCP103_2026_TP_GP1

## Requirements
- Python 3
- A LaTex compiler (like: MacTex, TexLive, etc)

## Start
- Install previous requirements
- Intall python dependencies: `pip install -r "requirements.txt"`
